const io = require('./index');

module.exports = function(socket) {
    console.log('socket ', socket.id);

    





//    let users = findAll();
  
  //  socket.on('online', ({ usuarioLogado }) => {
 //       removeUser(usuarioLogado);    
 //       addUser({'idUsuario':usuarioLogado, socket});
 //   });

 //   socket.on('sendMessage', obj => {
//        console.log(users);     
//        const {to, msg} = obj;

//        const user = getUser(to);

//        if(user) {
//            const {socket: socretUser} = user;

 //           socretUser.emit('messageSending', msg);
     //   }
   // });

}